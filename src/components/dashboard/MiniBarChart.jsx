// Commented out - replaced with simple icons
/*
import React from 'react'
import { Box } from '@adminjs/design-system'

export const MiniBarChart = ({ color = "#FF6B9D", data = [3, 5, 2, 4, 6, 3, 4] }) => (
  <Box width="100%" height="100%" display="flex" alignItems="end" gap="1px">
    {data.map((height, index) => (
      <Box
        key={index}
        width="8px"
        height={`${(height / Math.max(...data)) * 100}%`}
        bg={color}
        borderRadius="1px"
      />
    ))}
  </Box>
)
*/
